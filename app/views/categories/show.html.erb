<div class="page-header text-center">
	<h1>Categories#show <% if logged_in? and current_user.admin? %><span class="badge"><%= link_to '<i class="fa fa-edit"></i> Edit This Category'.html_safe, edit_category_path(@category) %> </span><% end %></h1>
</div>
<div class="text-center"><%= will_paginate @category_articles %></div>
<%= render 'articles/article', obj: @category_articles %>
<div class="text-center"><%= will_paginate @category_articles %></div>